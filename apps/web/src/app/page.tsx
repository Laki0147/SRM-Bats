import { HomeExperience } from '@/components/home/HomeExperience';

// The home renders in the active design variant (see HomeExperience +
// the global VariantSwitcher). Heritage is the default, unchanged look.
export default function Home() {
  return <HomeExperience />;
}
